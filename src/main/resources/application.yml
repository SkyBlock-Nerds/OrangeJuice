spring:
  application:
    name: OrangeJuice
  profiles:
    active: dev

server:
  port: 8080
  servlet:
    context-path: /api
  forward-headers-strategy: framework

springdoc:
  swagger-ui:
    path: /swagger
    enabled: true